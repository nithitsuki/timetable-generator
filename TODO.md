## common free time
- allow adding more than 2 classes to compare (2 by default)
- if class has A1/A2 type of batch, add option to show A1/A2/both


## add ics export option for timetable view
- copy the ics link instead of making user download the .ics file directly

## Improve timetabel view UX
- make Subjects card on the bottom collapsible (minimize/maximize button)
- make the ics link button have a calendar icon instead of download icon
- add a share icon next to the ics link to copy the page url to clipboard
- the top left back button is too small for mobile users, keep it somewhere else too

## Final polish
- add loading for timetabel view when fetching the timetable data wherever applicable
- improve timetable loading speed if possible (cache or smthn)